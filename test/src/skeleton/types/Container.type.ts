type ContainerType = {
    backGroundColor?: string,
    foreGroundColor?: string,
    height: number,
    width: number
    styles?: object
    children: object,
    minX?: number,
    minY?: number
}

export { ContainerType }